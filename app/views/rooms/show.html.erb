<body class="body_room">
<div id="splash_room">
<div id="splash-logo_room">読み込み中</div>
<!--/splash--></div>
<div class="splashbg_room"></div><!---画面遷移用-->

  <div id="container_room">

    <header id="header">
    <h1><%= "ルーム名：#{@room.name}" %></h1>
    <div class="">
    <%= link_to "トップページへ戻る", root_url, data: {turbolinks: false} %>
    </div>
    <%if current_user.id == @room.user_id %>
    <div class="">
      <%= link_to "ルームを削除する", room_path(@room), method: :delete, class:"question-destroy" %>
    <% end %>
    </div>
    </header>

  <main>
    <section class="quiz">
    <!--タイマー機能 -->
    <div class="timer">
      <form name="timer">
      <input type="text" value="">分
      <input type="text" value="">秒<br>
      <input type="button" value="スタート" onclick="cntStart()">
      <input type="button" value="ストップ" onclick="cntStop()">
      </form>
    </div>
    <!--タイマー機能 -->
    
    <div>      
     <!-- 問題出題 -->
     <%if current_user.id == @room.user_id %>
        <%= form_with model: [@room, @question], local: true do |f| %>
        <div id="q_select">
          <%= f.collection_select :quiz_id, @quizzes, :id, :title, {prompt: "問題を選択してください"} %>
          <%= f.submit '問題を出題する' %>
        </div>
        <% end %>
      <%end%>
     <!-- 問題出題 -->

     <!-- 問題表示 -->
      <div id="question" class="question">
        <%= render partial: 'questions/question', collection: @questions %>
      <div>
     <!-- 問題表示 -->
    </div>
    <!--/area2--></section>
    <section>
      <div class="wrapper_room">

        <div class="message_room">     
          <div id="messages" class="left-box">
            <%= render partial: 'messages/message', collection: @messages %>
          </div>
        </div>

        <div class="comment_room">
          <div id="comments" class="">
            <%= render partial: 'comments/comment', collection: @comments %>
          </div>
        </div>
      </div>
    <!--/area3--></section>

    <section>
    <%if current_user.id == @room.user_id %>

      <div>
      <div class="form">
        <h2>コメント入力フォーム</h2>
        <%= form_with model: [@room, @comment], class: '' do |f| %>
            <%= f.text_field :comment, class: 'form-comment', placeholder: 'YESかNOで返信してください' %>
          <%= f.submit '送信', class: 'form-submit' %>
        <% end %> 
      </div>
      </div>

    <%else%>
    <div class="form">
      <h2>メッセージ入力フォーム</h2>
      <%= form_with model: [@room, @message], class: '' do |f| %>
          <%= f.text_field :messages_content, class: 'form-message', placeholder: 'YESかNOで答えられる質問を入力してください' %>
        <%= f.submit '送信', class: 'form-submit' %>
      <% end %>
    </div>
    <% end %>
    <!--/area4--></section>
    <!--/contaier-->
  <!--/main--></main>
  </div> <!--/container-->
</body>